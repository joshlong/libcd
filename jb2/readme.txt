jb2 is the workspace where i'll renovate the libcdrip functionality and update it to include other features like transcoding. the code in the parent directory is the same as it always has been, and is used for reference and inspiration in this otheriwse new structure.

